defmodule MIx.Tasks.Detective.ImportStreetReportTest do
  use Detective.DataCase

  alias Mix.Tasks.Detective.ImportStreetReport

  test "creates street reports for each valid row from the file"
  test "logs every invalid row from the file"
  test "errors when the file does not exist"
end
